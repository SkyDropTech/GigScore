# Vercel Frontend Environment Configuration Example
# If backend is deployed separately (e.g. Render, Railway, AWS, Fly.io):
# Set VITE_API_URL to your deployed backend URL (e.g. https://gigscore-api.onrender.com)
# In local development or when using proxy rewrites, leave empty to default to /api.
VITE_API_URL=
